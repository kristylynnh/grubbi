photo_captions

<%= simple_form_for @photo, url: photo_captions_path(@place) do |f| %>
    <%= f.input :picture %>
    <%= f.input :caption %>
    <%= f.submit "Add a Photo", class: 'btn btn-primary' %>
  <% end %>